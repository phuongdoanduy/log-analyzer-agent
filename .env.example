# Vertex AI auth (Application Default Credentials via gcloud auth login --update-adc)
GOOGLE_GENAI_USE_VERTEXAI=TRUE
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=global

# MCP Server (optional)
# POLARIS_MCP_PORT=3003
